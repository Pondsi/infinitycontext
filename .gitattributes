# Keep every file byte-identical on checkout so that checksums.txt stays valid
# on every platform and every shell. Without this, core.autocrlf would rewrite
# line endings and every SHA-256 would differ.
* -text
