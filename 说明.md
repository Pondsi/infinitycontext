# InfinityContext — 无限上下文压缩与本地记忆归档

> 让长会话不再溢出：压缩上下文、把脱敏后的对话片段归档到**本地** SQLite/FTS5、随时精确检索。
> 可移植核心为纯 Python 标准库，**不联网、无 shell、无子进程、不常驻后台**。

> ⚠️ **能力披露（预期行为）**
>
> 1. **本地持久化**：把脱敏后的对话片段写入本地 SQLite/FTS5 归档（默认保留 30 天）。
> 2. **永久删除**：`cleanup.py` 会永久删除过期归档文件——必须存在 `.infinity-context-archive` 标记，且同时给出 `--apply --confirm-destructive`，不递归，只删本应用命名的文件。
> 3. **就地改写**：`--redact-file` 会原地改写文件——必须用 `--allow-dir` 限定范围。
> 4. **路径回退**：输出路径非 ASCII 时默认**拒绝**（退出码 8），需显式 `--allow-dir-fallback` 才改道并如实上报。
> 5. **随时可关**：设置 `INFINITY_CONTEXT_NO_ARCHIVE=1`，归档器不再写任何文件。
>
> 安装即表示接受以上本地持久化与文件修改能力。

## 这是什么？

InfinityContext 是一个标准 `SKILL.md` 格式的 AI Agent Skill，可运行在任何支持 Agent Skills 的宿主上——**原生支持 DeepSeek Harness（dsh）**（放入 `.agents/skills/` 即被自动发现），同样支持 Claude Code、OpenClaw、Cursor、Dify、Ollama 及自研 Agent。

它解决的核心问题：

```
用户发送消息 → 模型思考（消耗 20-30K tokens）→ 上下文溢出 → 对话崩溃/遗忘
```

做法：**压缩保持工作上下文小巧，归档保留历史细节**。需要旧细节时，用检索代替回放。

## 安装

### 前置条件

- 任意支持标准 `SKILL.md` / Agent Skills 的宿主：dsh、Claude Code、OpenClaw、Cursor、Dify、Ollama 或自研 Agent。
- **Python 3.9+**（仅标准库）。不需要 Node.js、不需要构建、不需要联网。

### 方式一：注册表安装（推荐）

```bash
clawhub install infinitycontext --workdir ~/.agents --dir skills    # dsh / Claude Code
clawhub install infinitycontext --workdir ~/.openclaw --dir skills  # OpenClaw
```

### 方式二：源码安装（固定 tag + 逐文件校验）

```bash
git clone https://github.com/Pondsi/infinitycontext.git
cd infinitycontext
git checkout --detach v1.8.4
grep -q '^version: "1.8.4"' SKILL.md || { echo "tag/version mismatch - stop"; exit 1; }
sha256sum -c checksums.txt          # macOS：shasum -a 256 -c checksums.txt
```

随后按 [`references/architecture.md`](references/architecture.md#file-layout) 的清单逐文件放入技能目录。

### 安装位置

| 宿主 | 位置 | 说明 |
|------|------|------|
| dsh | `~/.agents/skills/infinity-context/` 或 `<项目>/.agents/skills/infinity-context/` | 技能是**一层深的目录包**；嵌套 `**/SKILL.md` 故意不被发现 |
| OpenClaw | `<工作区>/skills/`（优先）或 `~/.openclaw/skills/` | 自动发现 |
| Claude Code | `~/.claude/skills/infinity-context/` | 同样结构 |
| Cursor / Dify / Ollama / 自研 | 让 Agent 指向本目录 | 纯 Python 标准库 |

> 可选的 OpenClaw/Windows 集成**不在本包内**，位于仓库 `openclaw/` 目录，安装方式见 `openclaw/README.md`。

## 快速上手

```bash
# 1. 归档一份会话轨迹（JSONL）
python3 scripts/session_to_sqlite.py \
  --session-key my-session \
  --session-file ~/path/to/events.jsonl \
  --output-dir ~/.infinity-context/archive

# 2. 以后精确检索（只读）
python3 scripts/search.py --db ~/.infinity-context/archive/my-session-*.db --query "部署令牌"

# 3. 按保留期清理归档文件（默认演练，不删任何东西）
python3 scripts/cleanup.py --archive-dir ~/.infinity-context/archive --dry-run
python3 scripts/cleanup.py --archive-dir ~/.infinity-context/archive --apply --confirm-destructive

# 4. 对既有归档执行保留期策略（不摄入新数据）
python3 scripts/session_to_sqlite.py --purge-only --output-dir ~/.infinity-context/archive
```

## 保留期与数据治理

| 设置 | 取值 | 说明 |
|------|------|------|
| 默认保留 | **30 天** | 每次运行都在同一事务内清理超期片段（含 FTS 镜像），结果返回 `purged_chunks` |
| 可配置范围 | `1..3650` 天 | `--retention-days N` |
| 不限时间 | **必须显式开启** | `--retention-days 0` 会被拒绝，除非同时给出 `--allow-unbounded-retention` |
| 手动清理 | `--purge-only --output-dir <目录>` | 对既有归档执行同一策略 |
| 彻底关闭 | `INFINITY_CONTEXT_NO_ARCHIVE=1` | 不写任何文件，返回 `status: disabled` |

保留期由**代码强制**，不是文档承诺。

## 安全与隐私

- **纯本地**：不联网、无遥测、无 MCP、无云同步。
- **无 shell、无子进程**：四个 Python 脚本从不启动其它程序；Windows ACL 使用进程内 Win32 安全 API。
- **归档仅本人可读（Fail-Closed）**：POSIX `0700`/`0600`；Windows 受保护 DACL；加固后回读校验；无法强制 owner-only 时中止并销毁半成品（退出码 3）；唯一降级开关是 `--allow-insecure-storage`，且结果中标记 `insecure_storage: true`。
- **脱敏 Fail-Closed**：入库前屏蔽 API Key / Token / 密码 / JWT / 私钥 / 连接串 / Cookie / Webhook / 手机号 / 邮箱；规则启动期校验并预编译；`session_key` 在生成任何路径/文件名之前先净化；高熵内容不进索引。
- **摄入有界**：文件大小 64 MiB / 单行 1 MiB / 消息 200000 条 / 累计字符 64 MiB，边读边限流，触顶即停并如实上报 `ingest.truncated`。
- **默认拒绝的文件系统规则**：`cleanup.py` 拒绝无标记目录与受保护目录（根、home、常见用户目录），只删完整文件名匹配的产物，不递归，删除前 `lstat` 复核，`VACUUM` 前只读校验 `session_chunks`/`chunk_fts`，且必须 `--apply --confirm-destructive`。

## 数据模型

| 表 | 用途 |
|----|------|
| `session_chunks` | 每个对话片段一行：`session_key`、`start_msg_id`、`end_msg_id`、`summary`、`keywords`、`anchor_questions`、`raw_content`、`created_at` |
| `chunk_fts` | FTS5（三元组）检索镜像，由 `AFTER INSERT` 触发器保持同步 |
| 索引 | `idx_session_key`、`idx_created_at`、`idx_chunk_unique(session_key, start_msg_id, end_msg_id)` |

`INSERT OR IGNORE` + 唯一索引让重复导入同一份轨迹保持幂等。

## 常见问题

**数据存在哪？** `--output-dir` 指定的目录（默认 `~/.infinity-context/archive`），一个 SQLite 数据库及 `-wal`/`-shm` 旁文件，owner-only 权限。

**怎么彻底停止归档？** 设置 `INFINITY_CONTEXT_NO_ARCHIVE=1`。

**怎么只保留更少历史？** `--retention-days 7`；或立即执行 `--purge-only --output-dir <目录>`。

**数据会外传吗？** 不会，包内没有任何网络代码。

**为什么非 ASCII 路径被拒绝？** Windows 上的 SQLite 无法使用该路径，默认退出码 8；`--allow-dir-fallback` 会改到 `~/.openclaw/sqlite-data` 并上报，绝不静默改道。

**支持哪些语言？** 除中英文外，另附 7 种语言的摘要，见 [`references/languages.md`](references/languages.md)。

## 已知限制

1. 核心是 Python 脚本，需要宿主提供 shell 工具来调用。
2. 脱敏是正则尽力而为，不是强加密；归档仍属敏感数据，请勿同步到云盘或放宽目录权限。
3. Windows 上的非 ASCII 输出路径不可用（见上）。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

- Bug 报告：<https://github.com/Pondsi/infinitycontext/issues>
- 提问与想法：<https://github.com/Pondsi/infinitycontext/discussions>

1. Fork 本仓库
2. 创建特性分支（`git checkout -b feature/amazing-feature`）
3. 提交更改（`git commit -m 'Add amazing feature'`）
4. 推送分支（`git push origin feature/amazing-feature`）
5. 创建 Pull Request

## 📄 许可证

MIT 许可证（附强制署名条款）——允许使用全部或部分源码（含修改后的变体），但必须标注 Pondsi 的署名。详见 [LICENSE](LICENSE)

## 🙏 致谢

- [OpenClaw](https://github.com/openclaw/openclaw) — AI 助手运行时框架
- 所有贡献者和用户

## 隐私声明 / Privacy Statement

本 Skill 将对话上下文保存在本机 SQLite 中以供检索，内置正则脱敏屏蔽常见 API 密钥，不依赖任何云端同步。所有数据仅存储于本机，不会外传。

This skill stores conversation context in a local-only SQLite database for retrieval. Built-in regex redaction masks common API keys. No cloud sync is used; all data stays on the local machine.

---

Pondsi (+MiMo-v2.5/v2.5pro+deepseek-v4-flash/pro+deepseek-v4.1-flash-expires-on-0910+GLM5.3-flash+Gemini3.1-pro+Qwen3.8-27b+Gemini3.8-flash) — automatically committed by Openclaw
