# InfinityContext — 无限上下文压缩与记忆优化

> 为所有 AI 智能体打造的 O(1) 恒定上下文、防爆栈的双层记忆引擎

> ⚠️ **隐私与数据留存声明**
>
> 1. **数据留存边界**：本插件不仅执行上下文压缩，还会在每次压缩前导出完整会话轨迹，并把脱敏后的对话片段写入本地 SQLite（用于 FTS5 检索）。
> 2. **数据最小化**：内置 `MAX_ARCHIVE_LENGTH` 强制截断（掐头去尾）+ 摄入前限流（文件大小 / 单行长度 / 消息条数 / 累计字符）+ 正则脱敏（API Key / Token / 密码 / JWT / 私钥 / 连接串 / 手机号 / 邮箱），高熵内容不进索引。
> 3. **纯本地存储**：数据仅存于本机 `%LOCALAPPDATA%\.openclaw\` 与 `%USERPROFILE%\.openclaw\`，不联网、不上传、无遥测；备份目录 ACL 收紧为「当前用户 + SYSTEM」。
> 4. **保留期**：轨迹备份默认保留 30 天，超期自动清理；可执行 `scripts/cleanup-old-backups.ps1` 手动清理并 VACUUM 回收空间。
> 5. **用户自主权**：自动恢复默认关闭，需在 `infinity-context.config.json` 中显式开启 `enableAutoWake`；每轮最多一次、执行前先写 `WAKE_REQUEST` 日志、绝不拉起通知进程；Agent 白名单默认拒绝。
> 6. **后台执行**：通过 OpenClaw 原生事件钩子在压缩前后自动运行，不会打断当前会话。

## 权限声明

| 能力 | 用途 |
|------|------|
| Shell / 进程 | 调用 `openclaw` CLI 与 PowerShell 辅助脚本（仅参数数组，无 shell 字符串拼接） |
| 文件读取 | 读取会话轨迹 JSONL 与本地配置 |
| 文件写入 | 写入 SQLite 归档、日志、脱敏后的轨迹备份（位于 `%LOCALAPPDATA%` / `%USERPROFILE%`） |
| 环境变量 | 读取 `LOCALAPPDATA`、`USERPROFILE`、`INFINITY_CONTEXT_AGENTS` |

**未声明 network / MCP：因为不使用。** 数据不离开本机。

> **注册表包说明**：ClawHub 等注册表不接受包含「自我执行 JavaScript」的包，因此本包**不含任何 `.js` 文件**。可选的 OpenClaw 压缩钩子（`openclaw/handler.js`、`openclaw/HOOK.md`、`openclaw/integrity.json`）**仅随 GitHub 仓库分发**。本包内所有内容都是「由 Agent 通过其声明工具调用」的脚本。

## 🎯 这是什么？

InfinityContext 是一个标准 `SKILL.md` 格式的 AI Agent Skill，可运行在任何支持 Agent Skills 的宿主上——**原生支持 DeepSeek Harness（dsh）**（放入 `.agents/skills/` 即被自动发现），同样支持 Claude Code、OpenClaw、Cursor、Dify、Ollama。它解决小模型（128K 上下文）对话中上下文溢出的问题，通过多层压缩机制让任何大小的模型都能持续对话而不中断。

### 核心问题

```
用户发送消息 → 模型思考（消耗 20-30K tokens）→ 上下文溢出 → 对话崩溃
```

### 解决方案

```
三层防护：
1. 配置层：keepRecentTokens=15000 + 看门狗阈值 35%
2. 管线层：自动备份→SQLite→压缩→唤醒
3. Hook 层：拦截所有压缩路径，确保数据不丢失
```

## 📦 安装

### 前置条件

- **任意支持标准 `SKILL.md` / Agent Skills 的宿主**——DeepSeek Harness（dsh）、Claude Code、OpenClaw、Cursor、Dify、Ollama 或自研 Agent。**OpenClaw 不是必需的。**
- Python 3.9+（脱敏引擎 + SQLite/FTS5 归档，可独立运行的核心）
- PowerShell 5.1+（仅 Windows，用于可选看门狗与压缩钩子）

### 步骤 1：安装可移植核心

```bash
# 方式一：注册表安装（已扫描产物，无需 git、无需构建）
clawhub install infinitycontext --workdir ~/.agents --dir skills

# 方式二：源码安装——固定已发布 tag（必须等于 SKILL.md 的 version），并逐文件校验
git clone https://github.com/Pondsi/infinitycontext.git
cd infinitycontext
git checkout --detach v1.6.3
grep -q '^version: "1.6.3"' SKILL.md || { echo "tag/version mismatch - stop"; exit 1; }
sha256sum -c checksums.txt          # macOS：shasum -a 256 -c checksums.txt

# 逐文件显式复制（禁止 cp -r、禁止通配符）
mkdir -p ~/.agents/skills/infinity-context/scripts
mkdir -p ~/.agents/skills/infinity-context/references
cp SKILL.md README.md 说明.md CHANGELOG.md SPONSORS.md LICENSE ~/.agents/skills/infinity-context/
cp scripts/session_to_sqlite.py scripts/search.py scripts/cleanup.py scripts/secure_fs.py ~/.agents/skills/infinity-context/scripts/
cp references/architecture.md references/languages.md ~/.agents/skills/infinity-context/references/
```

dsh 与 Claude Code 共用 `~/.agents/skills`；技能是**一层深的目录包**（`<root>/<dir>/SKILL.md`），**不支持递归发现**（嵌套的 `**/SKILL.md` 故意不被发现）；frontmatter 的 `name`/`description` 必填，`name` 须为 kebab-case；文件夹名不必与 `name` 相同（dsh 以 frontmatter 的 `name` 为标识）。

### 步骤 2（可选）：OpenClaw 自动化

Windows + OpenClaw 专有的压缩钩子与看门狗**不在本包内**，位于仓库 `openclaw/` 目录，
安装方式见 `openclaw/README.md`（固定版本 + SHA-256 校验 + 逐文件复制，禁止通配符）。

在 `openclaw.json` 中添加：

```json
{
  "agents": {
    "defaults": {
      "compaction": {
        "mode": "safeguard",
        "keepRecentTokens": 15000,
        "model": "tokease/deepseek-v4-flash",
        "timeoutSeconds": 600,
        "midTurnPrecheck": { "enabled": true }
      }
    }
  },
  "hooks": {
    "internal": {
      "entries": {
        "compaction-pipeline": { "enabled": true }
      },
      "load": {
        "extraDirs": ["~/.openclaw/hooks"]
      },
      "enabled": true
    }
  }
}
```

### 步骤 3：调度（可选）

**本插件依赖 OpenClaw 原生事件钩子运行，无需在系统中安装计划任务后台服务。**

可选：如需看门狗定期检查（非必需），请使用 OpenClaw 自带的调度能力（`automations`），
不要创建系统级计划任务。

### 步骤 4：重启 Gateway

```bash
openclaw gateway restart
```

## 🔧 工作原理

### 压缩管线

```
任意压缩触发（手动/自动/看门狗）
  │
  ├─ compact:before → pipeline.ps1
  │   ├─ export-trajectory（完整轨迹备份）
  │   └─ session-to-sqlite（JSONL → SQLite + FTS5）
  │
  ├─ OpenClaw 执行压缩
  │
  └─ compact:after → pipeline.ps1
      └─ 增强摘要（从 SQLite 生成关键词索引）
```

### 去重机制

```
5 分钟内已有备份？
  ├─ 是 → 跳过（避免重复工作）
  └─ 否 → 执行备份+SQLite
```

### 空间保障（128K 模型）

```
压缩后上下文：
  系统提示 + 工具  ≈ 30K tokens
  摘要             ≈ 7K tokens
  最近对话         = 15K tokens
  ─────────────────────
  总计             = 52K / 131K (40%)
  剩余空间         = 79K tokens

最坏情况单次回复：
  thinking: 30K + 回复: 10K + 工具: 15K = 55K
  79K > 55K → 保证至少 1 次完整深度回复
```

## ⚙️ 配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| keepRecentTokens | 15000 | 压缩后保留的 tokens 数 |
| ThresholdPct | 35.0 | 看门狗触发阈值（%） |
| ThresholdAbsTokens | 60000 | 绝对值门槛 |
| CompactCooldownMin | 5 | 压缩冷却时间（分钟） |
| StickyLimit | 5 | 连续失败暂停阈值 |
| StickyPauseMin | 30 | 暂停时长（分钟） |
| AllowedAgents | `main` | **默认拒绝**：仅处理白名单内的 Agent（见下） |

### Agent 白名单配置（默认拒绝）

看门狗与 hook 管线只处理白名单内的 Agent，**不存在“留空 = 全部允许”的降级**。解析优先级：

1. 环境变量 `INFINITY_CONTEXT_AGENTS`（逗号分隔，例：`main,yai`）
2. 配置文件 `%LOCALAPPDATA%\.openclaw\infinity-context.config.json`：

```json
{ "allowedAgents": ["main", "yai"] }
```

3. 内置默认值：`@('main')`

若解析结果为空（例如配置文件显式写成 `[]`），脚本会记录 `SECURITY_ABORT` 并直接退出，**绝不回退为全部允许**。

### 备份清理脚本安全约束

`scripts/cleanup-old-backups.ps1` 仅清理同时满足以下全部条件的文件：

- 保留天数在 `1..3650` 之间（拒绝 0 与负数）
- 规范化后的路径必须位于 `%LOCALAPPDATA%\.openclaw\backups` 之内（分隔符锚定，`backups-evil` 无法绕过）
- 扩展名属于 `.jsonl .db .db-wal .db-shm .bak .tmp .json`
- 不是符号链接 / Junction（ReparsePoint）

支持 `-WhatIf` 预演：

```powershell
powershell -NoProfile -File scripts\cleanup-old-backups.ps1 -RetentionDays 30 -WhatIf
```

## 🗄️ SQLite FTS5 搜索

压缩后的会话存储在 SQLite 中，支持两种搜索：

```sql
-- FTS5 trigram 搜索（≥3 字符）
SELECT * FROM chunk_fts WHERE chunk_fts MATCH '关键词';

-- LIKE fallback（中文 2 字）
SELECT * FROM session_chunks WHERE raw_content LIKE '%关键词%';
```

## 🐛 已知限制

1. **CLI compact 可能失败**：`openclaw sessions compact` 可能返回 "changed before compaction" 错误。这是 OpenClaw 的保护机制，非本 skill 的 bug。
2. **自动压缩依赖模型配额**：压缩使用配置的模型，如果模型 API 限流（429），压缩会失败。
3. **Windows 专用**：PowerShell 脚本目前仅支持 Windows。Linux/macOS 用户需要自行适配。

## 📝 更新日志

### v7.1 (2026-09-08)
- **T05 默认拒绝**：白名单为空不再降级为“全部允许”，改为安全阻断退出
- **T05 移除全盘扫描**：pipeline.ps1 不再枚举 `~/.openclaw/agents`，改为按授权 Agent 精准查询
- **T05 agentId 推导**：会话缺少 agentId 时从 session key 推导，仍须通过白名单校验
- **T09 清理脚本硬化**：保留天数范围校验 + 路径 Canonical 锚定 + 扩展名白名单 + 跳过符号链接 + `-WhatIf`
- 新增 `infinity-context.config.json` 本机白名单配置（不随仓库分发）

### v6.8 (2026-09-08)
- 统一备份管线：看门狗不再重复做 backup+SQLite
- 新增去重机制：5 分钟内已有备份则跳过

### v6.7 (2026-09-08)
- 新增 compaction-pipeline hook：拦截所有压缩路径
- 修复 handler.js exports.default 导出
- 修复 pipeline.ps1 Python 字符串转义问题
- 修复 task-recovery HOOK.md frontmatter

### v6.6 (2026-09-08)
- 修复 sticky pausedUntil 写入
- 修复 sticky [long] 时间戳溢出

### v6.5 (2026-09-08)
- SqliteDir 统一 ASCII 路径
- session_chunks 表名修正

### v6.4 (2026-09-08)
- 冒号路径修复（Windows 目录名禁止冒号）

### v6.3 (2026-09-08)
- UTF-8 编码强制
- 控制字符清理

### v5.9 (2026-09-08)
- 双条件触发（%+绝对值）
- session_chunks 表名

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

## 📄 许可证

MIT 许可证（附强制署名条款）——允许使用全部或部分源码（含修改后的变体），但必须标注 Pondsi 的署名。详见 [LICENSE](LICENSE)

## 🙏 致谢

- [OpenClaw](https://github.com/openclaw/openclaw) — AI 助手运行时框架
- 所有贡献者和用户

## 隐私声明 / Privacy Statement

本 Skill 会将对话上下文保存在本地纯内网的 SQLite 中以供检索，系统已内置正则脱敏机制屏蔽常见 API 密钥，且不依赖任何云端同步。所有数据仅存储于本机，不会外传。

This skill stores conversation context in a local-only SQLite database for retrieval. Built-in regex redaction masks common API keys. No cloud sync is used; all data stays on the local machine.

---

Pondsi (+MiMo-v2.5/v2.5pro+deepseek-v4-flash/pro+/deepseek-v4.1-flash-expires-on-0910+GLM5.3-flash+Gemini3.1-pro+Qwen3.8-27b) — automatically committed by Openclaw
